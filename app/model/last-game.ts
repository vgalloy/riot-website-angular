import {Region} from "./region";
export class LastGame {
    assist:number;
    championId:number;
    death:number;
    gameId:number;
    kill:number;
    region:Region;

    visible:boolean; // TODO C'est juste horrible
}